<div class="jumbotron">
  <h1 class="display-4"><%= @booking.full_name %></h1>
  <p class="lead"><%= @booking.email %></p>
  <hr class="my-4">
  <p><%= @booking.room %></p>
  <p class="lead"><%= @booking.date %></p>
</div>



<%= link_to 'Editar reserva', edit_booking_path(@booking), class:"btn btn-secondary" %>
<%= link_to 'Voltar', bookings_path, class:"btn btn-dark" %>
